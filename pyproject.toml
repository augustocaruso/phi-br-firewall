[project]
name = "phi-br-presidio-firewall"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
  "click",
  "presidio-analyzer",
  "presidio-anonymizer",
  "pydantic>=2",
  "pyyaml",
  "typer",
  "rich",
  "babel>=2.18.0",
]

[project.optional-dependencies]
nlp = [
  "spacy>=3.8,<4",
]

[project.scripts]
phi = "phi_br_core.cli.main:app"

[project.urls]
Homepage = "https://github.com/augustocaruso/phi-br-firewall"
Repository = "https://github.com/augustocaruso/phi-br-firewall"
Issues = "https://github.com/augustocaruso/phi-br-firewall/issues"

[dependency-groups]
dev = [
  "pytest",
  "ruff",
  "mypy"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["packages/phi_br_core/phi_br_core"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["packages/phi_br_core"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["phi_br_core"]
